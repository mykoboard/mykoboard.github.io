import React from 'react';
import ReactDOM from 'react-dom/client';
import StandaloneApexNebula from './StandaloneApexNebula';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
    <React.StrictMode>
        <StandaloneApexNebula />
    </React.StrictMode>
);
